// @flow
import React, { Fragment } from "react";

type PropTypes = {};

export const AdminPanel = (props: PropTypes) => {
	return (
		<Fragment>
			<h1>Admin Panel</h1>
		</Fragment>
	);
};

export default AdminPanel;
